#define name mail-stop
#define glyph envelope
#define color red

#include 'lib/base.cherri'
#include 'lib/appids.cherri'

quitApp(APPID_TEAMS)
quitApp(APPID_OUTLOOK)
quitApp(APPID_MATTERMOST)
quitApp(APPID_TIMELOGGER)

paste gobble
